services:
  kasflow:
    build: .
    container_name: kasflow-bot
    restart: unless-stopped
    volumes:
      - ./src/kasflow:/app/src/kasflow
      - ./data:/app/data
    ports:
      - 8443:8443
    environment:
      - DATA_DIR=/app/data
    env_file:
      - .env 